import SocketUtils

walletList = ['localhost']
def minerServer(myIP, walletList):
    #Open Server Connection
    server = SocketUtils.newServerConnection()

    #recieve two transactions

    #collect those into a block

    #Mine (i.e. find a satisfying nonce)

    #send that block to each in walletList
    return False

